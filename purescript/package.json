{
  "name": "purescript",
  "packageManager": "yarn@3.2.1",
  "devDependencies": {
    "parcel": "^2.6.2"
  },
  "scripts": {
    "develop": "parcel index.html",
    "build": "npx spago build && parcel build index.html --public-url /purescript"
  }
}
